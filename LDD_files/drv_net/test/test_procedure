// Load the virtual network device driver. 
sudo insmod drv_net1.ko

// Check whether two network interfaces vnd0 and vnd1 have been created.
ifconfig

// Configure network interface vnd0 with IP address : 192.168.0.1
// Configure network interface vnd1 with IP address : 192.168.1.2
// For doing so, copy/overwrite the given interfaces file at /etc/network dir.
// Restart the Networking Service deamon.
sudo systemctl restart networking.service

// Check whether IP address is configured to network interfaces vnd0 & vnd1.
ifconfig

// Due to partial implementation of network device driver, the networking
// service daemon unconfigure the network interfaces after some time.
// IP address might disappear after some time. In such a situation re-run
// the systemctl command. Sometimes, it might not help and you have to run
// the following commands.
sudo ifdown vnd0
sudo ifup vnd0
sudo ifdown vnd0
sudo ifup vnd0

// Run networking packet tracing tool - tcpdump to capture the packets
// on another terminal.
sudo tcpdump -i vnd0 -vv 
// and/or
sudo tcpdump -i vnd1 -vv 

// Run server on one terminal.
sudo ./server

// Run client on another terminal.
sudo ./client

// Enter a message "Hello server" on client side when prompted.
// The above message should appear on the server side.

// Enter a message "Hello client" on server side when prompted.
// The above message should appear on the client side.

// Unload the virtual network device driver. 
sudo rmmod drv_net1

// Check whether the two network interfaces still exist.
ifconfig
// The two network interfaces should have disappeared.
